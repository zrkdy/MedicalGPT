# 评测驱动训练额外依赖

# 向量化模型 - 二选一
zhipuai>=2.0.0              # GLM-embedding-3 (推荐，需要API Key)
sentence-transformers>=2.2.0  # 本地向量化模型 (免费)

# 向量计算
numpy>=1.21.0
faiss-cpu>=1.7.0            # 可选：大规模向量召回加速

# 数据处理
pandas>=1.3.0
tqdm>=4.62.0
datasets>=2.0.0

# 基础依赖（如果没有安装）
torch>=2.0.0
transformers>=4.35.0
